import { ArrayconverterPipe } from './arrayconverter.pipe';

describe('ArrayconverterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayconverterPipe();
    expect(pipe).toBeTruthy();
  });
});
